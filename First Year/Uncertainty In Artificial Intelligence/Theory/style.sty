% Imports
\usepackage{graphicx, array, amsthm, amssymb, amsmath, algorithm, algpseudocode, float, xcolor, thmtools, thmbox, chronology, multirow, tikz, listings, color, blindtext, geometry, fancyhdr, nameref}
\usepackage[english]{babel}

% Page size settings
\geometry{
  a4paper, 
  left=2cm, 
  right=2cm, 
  top=2.5cm, 
  bottom=2.5cm
}


\pagestyle{fancy}
\setlength{\headheight}{15pt}
\fancyhf{} % Clear header and footer
\fancyheadoffset[R]{0pt} % Adjust header position to match text width
\fancyhead[L]{\nouppercase{\rightmark}} % Left header - Section Title
\fancyhead[R]{\thepage} % Right header

\makeatletter
\let\ps@plain\ps@empty
\makeatother


% Theorems
\makeatletter
\renewcommand\thmbox@headstyle[2]{\bfseries #1}
\makeatother
\newtheorem[style=M,bodystyle=\normalfont]{theorem}{Theorem}
\newtheorem[style=M,bodystyle=\normalfont]{corollary}{Corollary}
\newtheorem[style=M,bodystyle=\normalfont]{lemma}{Lemma}
\newtheorem[style=M,bodystyle=\normalfont]{definition}{Definition}

% Cross
\newcommand{\tikzxmark}{%
\tikz[scale=0.23] {
    \draw[line width=0.7,line cap=round] (0,0) to [bend left=6] (1,1);
    \draw[line width=0.7,line cap=round] (0.2,0.95) to [bend right=3] (0.8,0.05);
}}

% Colors
\definecolor{my_green}{rgb}{0,0.6,0}
\definecolor{my_gray}{rgb}{0.4,0.4,0.4}
\definecolor{my_variables}{rgb}{0.58,0,0.82}
\definecolor{my_keywords}{rgb}{0.3,0,0.82}

% Java listings
\lstdefinestyle{Java}{
  language=Java,
  basicstyle={\small\ttfamily},
  belowskip=3mm,
  breakatwhitespace=true,
  breaklines=true,
  classoffset=0,
  columns=flexible,
  commentstyle=\color{my_green},
  keywordstyle=\color{blue},
  numbers=none, 
  numberstyle=\tiny\color{my_gray},
  showstringspaces=false,
  stringstyle=\color{my_variables},
  tabsize=3,
  xleftmargin=1em,
  emph={@Entity, @Id, @GeneratedValue, @Temporal, @Basic, @LOB, @Column, @Transient, @ManyToOne, @JoinColumn, @OneToMany, @OneToOne, @ManyToMany, @JoinTable, @Stateless, @PersistenceContext},
  emphstyle={\color{my_gray}},
}

% SQL listings
\lstdefinestyle{SQL}{
  language=SQL,
  basicstyle={\small\ttfamily},
  belowskip=3mm,
  breakatwhitespace=true,
  breaklines=true,
  classoffset=0,
  columns=flexible,
  commentstyle=\color{my_green},
  keywordstyle=\color{blue},
  numbers=none, 
  numberstyle=\tiny\color{my_gray},
  showstringspaces=false,
  stringstyle=\color{my_variables},
  tabsize=3,
  xleftmargin=1em,
  morekeywords={FETCH, SKYLINE, OF, DIFF}
}

% MATLAB listings

% PYTHON listings